#include <stdio.h>
#include <stdlib.h>
#include <math.h>

/*
// Dane są długości boków. Proszę obliczyć pole i obwód prostokąta.
int main()
{
    float x,y,pole,obwod;
    printf("\nWprowadz dlugosc 1 boku ");
    scanf("%f",&x);
    printf("\nWprowadz dlugosc 1 boku ");
    scanf("%f",&y);
    pole=x*y;
    obwod=2*(x+y);
    printf("\npole prostokata = %f",pole);
    printf("\nobwod prostokata = %f",obwod);
    return 0;
} */

/*
// Dany jest promień koła. Proszę obliczyć pole i obwód koła (π -> M_PI z biblioteki math.h)
int main()
{
    float R,pole,obwod;
    printf("\nWprowadz promien kola ");
    scanf("%f",&R);
    pole=M_PI*R;
    obwod=2*M_PI*R;
    printf("\npole kola = %f",pole);
    printf("\nobwod kola = %f",obwod);
    return 0;
}
*/

/*
//Dane są 2 oceny: z egzaminu pisemnego i ustnego. Proszę obliczyć średnią arytmet. z tych ocen.
int main()
{
    float pis,us,s_aryfmetyczna;
    printf("\nwprowadz ocenu z egzaminu pisemnego ");
    scanf("%f",&pis);
    printf("\nwprowadz ocenu z egzaminu ustnego ");
    scanf("%f",&us);
    s_aryfmetyczna=(pis+us)/2;
    printf("\nocena aryfmetyczna %f",s_aryfmetyczna);
}*/

/*
//obliczyć i wyświetlić wartość wyrażenia:
int main(){
    float x,a,b;
    printf("\nWpisz a ");
    scanf("%f", &a);
    printf("\nWpisz b ");
    scanf("%f",&b);
//    x=sqrt((2*a*a)+(b*b));
//    x=(a*sin(b))/(pow(27, 1./3));
//    x=sqrt(fabs(a-b))+pow(a, 3);
//    x=cos(2*b)-sqrt(pow(a, 2))+1;
//    x=(a+sin(b))/((fabs(pow(a, 3)))+1);
//    x=pow((a-(b*cos(a))),1./3);
//    x=a+sqrt(pow(a,2)+((pow(a,2)-a)/3));
//    x=(2./3)*pow((a+b),3)-sqrt(fabs(a))+(10*b);
    printf("\nx = %f",x);
}*/
